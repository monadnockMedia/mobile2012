package com.mobile.signalFlags {		import flash.display.MovieClip;	import com.nestor.elements.maskedDisplacement;		public class jib2 extends maskedDisplacement {						public function jib2() {			// constructor code			super();			speedY = 5.767; 			channel = 1; 			flapX = 20; 			flapY = 40; 			baseX = 478; // size of noise in x axis			baseY = 130; // size of noise in y axis			octs = 1; // noise functions for noise (smaller = faster)			seed = Math.floor(Math.random() * 99); // random seed for noise			noiseWidth=noiseLength=109;						link(this.jib2_mc, this.displace_mc);			build();			start();		}	}	}