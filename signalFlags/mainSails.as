package com.mobile.signalFlags {		import flash.display.MovieClip;	import com.nestor.elements.maskedDisplacement;		public class mainSails extends maskedDisplacement {						public function mainSails() {			// constructor code						super();			speedY = 7; 			channel = 1; 			flapX = 40; 			flapY = 20; 			baseX = 500; // size of noise in x axis			baseY = 100; // size of noise in y axis			octs = 1; // noise functions for noise (smaller = faster)			seed = Math.floor(Math.random() * 22); // random seed for noise			noiseWidth=1;			noiseLength=700;						link(this.sail_mc, this.displace_mc);			build();			start();											}	}	}