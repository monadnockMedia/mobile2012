package  {	import flash.display.MovieClip;		///Class to manage wck objects, add functions to add/remove delItems based on arrays passed from stack	public class itemManager {		var world:MovieClip;		var worldItems:Array(); // this array contains all items currently visible.		var focusItem:delItem; //this is the item that currently has focus, or null if no item has focus.		public function itemManager(_world:Movieclip) {			// constructor code			world = _world;		}				public function addItems(aI:Array):void		{			for (var i:Number = 0; i <= aI; i++)			{				var tmpItem:delItem = new delItem();								var lastI = theItems.push(tmpItem);								addChild(tmpItem);				wUtil.randomShove(tmpItem);				}		}		}	}